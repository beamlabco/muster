version: 2

project_name: muster

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/muster
    binary: muster
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - repository:
      owner: beamlabco
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/beamlabco/muster"
    description: "CLI tool for team standups, attendance tracking, and leave management"
    license: "MIT"
    install: |
      bin.install "muster"
    test: |
      assert_match version.to_s, shell_output("#{bin}/muster --version 2>&1", 1)
